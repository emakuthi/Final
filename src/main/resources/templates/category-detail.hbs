{{#partial "content"}}
    <h1>{{category.name}}</h1>
    <p>Here are all the Sites listed in this Engineer:</p>
    <ol>

        {{#each tasks}}
            <li>
                <p><a href="/categories/{{category.id}}/tasks/{{id}}">{{description}}</a></p>
            </li>
        {{/each}}
    </ol>
    <a class="btn btn-warning btn-sm" href="/categories/{{category.id}}/edit">edit Engineer</a>
    <a class="btn btn-warning btn-sm" href="/categories/{{category.id}}/delete">delete Engineer</a>
{{/partial}}

{{> layout.hbs}}